class Maze {
  // map: number[];
  // enter: { x: number, y: number };
  // exit: { x: number, y: number };

  constructor(public map: number[][], public enter: { x: number, y: number }, public exit: { x: number, y: number }){

  }

}